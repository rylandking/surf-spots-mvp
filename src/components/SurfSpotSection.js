import * as React from 'react';

export default function SurfSpotSection({ titleContraction }) {
    return (
        <div>
            <p>{titleContraction} surf is a right reef break.</p>
        </div >
    )
}