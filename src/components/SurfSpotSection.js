import * as React from 'react';

export default function SurfSpotSection({ titleContraction }) {
    return (
        <div>
            <p>{titleContraction} surf is ${skillPhrase} right reef break.</p>
        </div >
    )
}